/**
 * @deprecated Use `getStagePresentation` from `@/util/calendarUtils` instead.
 * Este arquivo existe apenas para evitar erros de import em migrações pendentes.
 */
export { getStagePresentation as default } from '@/util/calendarUtils';
