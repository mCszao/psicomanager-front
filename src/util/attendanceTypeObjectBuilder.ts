/**
 * @deprecated Use `getTypePresentation` from `@/util/calendarUtils` instead.
 * Este arquivo existe apenas para evitar erros de import em migrações pendentes.
 */
export { getTypePresentation as default } from '@/util/calendarUtils';
